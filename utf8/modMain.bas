Attribute VB_Name = "modMain"
Option Explicit

Sub Main()
    ' Dim totalAmount As Long
    ' totalAmount = 1
    
    
    ' ReDim m_arrEntries(0 To -1)
    ' ReDim m_arrEntries(0 To 1)
    Dim m_arrEntries(0 To 1)
    
End Sub
